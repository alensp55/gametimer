[English](README.md) | **Русский**

# OneMoreTimer — предварительная версия 0.6.1

Небольшой офлайн-таймер приложений для Windows 10/11 x64: показывает время в выбранных приложениях за сегодня
и мягко предупреждает о дневном ориентире. По желанию можно включить принудительное завершение приложений
по индивидуальному дневному лимиту или расписанию. По умолчанию эти правила выключены.

Интерфейс доступен на английском (по умолчанию) и русском.
Для переключения: **General → Language → Русский → Apply**.
В общих настройках находятся общий интервал опроса и автозапуск. Режим учёта и правила задаются для каждого приложения.

## Запуск готовой программы

**[Скачать OneMoreTimer 0.6.1 для Windows x64 (.exe)](https://github.com/alensp55/OneMoreTimer/releases/download/v0.6.1/OneMoreTimer.exe)**

Все версии доступны на [странице релизов](https://github.com/alensp55/OneMoreTimer/releases).
Локальная сборка находится в `artifacts\win-x64-0.6.1\OneMoreTimer.exe`.
Для self-contained версии установка .NET не нужна.

1. Добавьте имена игровых `.exe` вручную или кнопкой «Выбрать .exe».
2. Выберите приложение и настройте справа учёт времени, дневной лимит и завершение по расписанию.
   Кнопка над списком меняет ориентир общего таймера. Интервал опроса находится в «Общих настройках».
3. На вкладке «Окно таймера» выберите монитор, угол, отступы и непрозрачность.
   «Предпросмотр на 15 секунд» показывает окно даже без запущенной игры.
4. «Применить» сохраняет параметры и оставляет настройки открытыми.
   «ОК» дополнительно закрывает настройки; иконка остаётся в системном трее.
5. Автозапуск включается только флажком «Запускать вместе с Windows» и сохранением настроек.
   Из автозапуска приложение открывается сразу в трее, даже если файл настроек отсутствует.
   При ручном запуске без `--tray` открывается окно настроек.

Двойной щелчок по значку tray открывает настройки. Контекстное меню содержит паузу и выход.
Пауза приостанавливает и учёт, и принудительное завершение; продолжение сразу проверяет включённые правила.
Крестик окна настроек закрывает его без сохранения внесённых в форму изменений, но оставляет мониторинг в фоне.
Повторный запуск не создаёт второй экземпляр в той же пользовательской сессии.

## Как считается время

- Для каждого приложения можно считать только активное окно, всё время работы (включая фон) или отключить учёт.
  Новые приложения по умолчанию учитываются по активному окну. При обновлении прежний режим сохраняется.
- Сопоставление имён файлов не зависит от регистра. Полный путь не сохраняется.
- Интервал опроса настраивается от 1 до 3600 секунд, по умолчанию — 60.
  При запуске, смене настроек, при системных событиях выполняются дополнительные проверки.
- Каждый интервал относится к состоянию на предыдущем опросе. Это приближённая оценка:
  короткая игра может быть пропущена, после закрытия игры возможен лишний интервал опроса.
  При частых переключениях ошибка накапливается. Это не точный секундомер активности.
- Если одновременно работают несколько игр, отдельное время пополняется у каждой,
  но общий дневной итог растёт только один раз за интервал.
- На экране показаны полные часы и минуты. В JSON сохраняются секунды с дробной частью.
- Сон, гибернация, блокировка/отключение пользовательской сессии и ручная пауза исключаются.
- Программа не восстанавливает игру за время, пока сама была выключена.

День определяется локальной датой Windows. В полночь дневные данные заменяются новыми.
При следующем запуске также проверяется дата. Отдельной истории дней нет.
Перевод часов или часового пояса не создаёт игровое время: интервал на таком переходе пропускается.
При переходе на другую календарную дату начинается новый счётчик, в том числе при ручном переводе даты назад.
Промежутки длиннее 1,5 интервала опроса (но не менее 5 секунд) также пропускаются.

Вкладка «Приложения» объединяет все приложения и сегодняшнюю статистику.
Выберите строку — справа появятся её настройки. Флажок слева от имени включает или приостанавливает
все правила приложения, сохраняя параметры и накопленное время. Колонка «Правила» кратко описывает поведение.
У приложения, нужного только для завершения по расписанию, учёт можно оставить выключенным.

«Изменить время…» исправляет сегодняшнее время. «Удалить» убирает приложение, его правила и дневную строку.
Эти два действия сохраняются сразу и пересчитывают общий итог; изменения правил ждут «Применить».
При последовательной работе итог меняется на разницу старого и нового времени строки.
Точные пересечения одновременной работы не хранятся: итог ограничивается временем самой долгой оставшейся
строки снизу и суммой строк сверху. Удаление последней строки обнуляет итог.

## Лимиты и расписания приложений

- Включите «Дневной лимит», задайте часы и минуты (от 1 минуты до 24 часов) и выберите «Предупредить»
  или «Завершить приложение». Используется время выбранного приложения, независимо от общего ориентира.
  Предупреждение показывается через трей при достижении лимита; его видимость зависит от настроек уведомлений Windows.
  Отключение учёта также выключает дневной лимит.
- «Завершать по расписанию»: выключено, общее расписание или своё расписание.
  Кнопка «Изменить расписание…» открывает запрещённые интервалы.
  Изменение общего расписания действует на все приложения, выбравшие его; своё относится только к выбранному.
  Расписание работает и без учёта времени.
  Время местное: начало включается, конец — нет. Понедельник 22:00–07:00 заканчивается во вторник утром.
  «Весь день» означает выбранные календарные дни.
- Правила действуют после «Применить» или «ОК», в том числе если лимит уже достигнут.
  Учёт, лимиты и расписание используют общий интервал опроса.
  Повторно запущенное приложение снова завершается на проверке, пока действует правило.
  Дневной лимит начинается заново в полночь.
- Старые настройки переносятся автоматически: списки объединяются, прежние режимы учёта и включённые лимиты
  сохраняются, приложения с включённым расписанием используют общее расписание.
  Приложения только из расписания не начинают учитывать время. Ранее выключенное завершение остаётся выключенным.

Принудительное завершение может прервать матч и потерять несохранённые данные. Завершаются все процессы
с указанным именем `.exe` в текущем сеансе Windows, включая фоновые; дочерние процессы с другими именами не затрагиваются.
Саму утилиту и основные системные процессы нельзя назначить целями завершения.
Права администратора не запрашиваются. Ошибки доступа показываются в трее и настройках.
Пауза или выход из OneMoreTimer прекращают действие правил. Это добровольный контроль, без защиты от обхода.

## Таймер

Таймер показывает только часы и минуты: `01:37`, без заголовка и полосы прогресса.
Окно подстраивается под ширину строки и имеет небольшой отступ вокруг неё.
Если ориентир включён, формат — `01:37 / 02:00`; после превышения — `02:17 / 02:00 +17 мин`.
При достижении ориентира используется розовый цвет и необязательная плавная пульсация с периодом 4 секунды.
Предупреждения на 80% нет. Без ориентира нет ни выделения, ни пульсации.
Ориентир от 1 до 1440 минут; в новых настройках он выключен. В JSON значение 0 означает выключенный ориентир.
При обновлении сохранённый ранее ориентир остаётся включённым; его можно отключить в окне «Изменить ориентир…».
Звуков нет. Сам по себе общий ориентир таймера не завершает приложения.

По умолчанию включён флажок «Только при запущенной игре»: окно показывается при обнаруженной игре
и скрывается после опроса, на котором игры уже нет. Проверяется запуск игры, даже если режим учёта
выбран «Только когда окно игры активно», а игра находится в фоне.
Если снять этот флажок, таймер отображается и без игры. Пауза, сон и блокировка сеанса скрывают окно.
При выключенном отображении учёт продолжается. Окно не получает фокус и пропускает клики.
Позиция меняется в настройках, перетаскивание во время игры не предусмотрено.

Оконный режим и borderless — целевые режимы для показа поверх игры.
В настоящем exclusive fullscreen отдельное Windows-окно может не отображаться поверх игры:
используйте второй монитор или borderless. Программа не вмешивается в режим вывода игры.
Режим «Монитор игры (авто)» запоминает экран последней обнаруженной активной игры,
а до первого такого наблюдения использует основной экран.
При отключении выбранного монитора окно переносится на основной; ранее выбранный экран остаётся в настройках.
Чтобы обновить список доступных мониторов после подключения, заново откройте настройки.

## Данные и доверие

Данные остаются в `%LocalAppData%\GameTime` для совместимости с прежними версиями.
Переименование сохраняет настройки и сегодняшнюю статистику:

- `settings.json` — параметры приложения;
- `today.json` — текущая дата, общий итог и время отдельных игр;
- `.bak` — предыдущая сохранённая копия для восстановления, это не архив дневной истории.

При частом опросе статистика записывается не чаще раза в минуту; при интервале больше минуты — каждый опрос.
Ручные правки, пауза и штатный выход сохраняют данные сразу.
Используются временный файл, сброс записи на диск и атомарная замена файла.
При аварии возможна потеря времени после последнего сохранения, включая текущий ещё не опрошенный интервал;
при отключении питания или повреждении диска абсолютная сохранность не гарантируется.
Если основная копия повреждена, приложение пробует резервную и показывает предупреждение.
Повреждённый файл сохраняется с суффиксом `.damaged-...`. Если исправных копий нет,
программа сообщает ошибку и не обнуляет их молча. Ошибка записи отображается через tray и настройки.

У приложения нет сетевых запросов, телеметрии, обновлений, аккаунтов, служб и драйверов.
Не используются DLL injection, графические/input hooks, чтение или изменение памяти игр.
WinAPI запрашивает имя foreground-процесса с `PROCESS_QUERY_LIMITED_INFORMATION`;
для обнаружения запущенных игр используется стандартное перечисление имён процессов .NET.
Включённые правила завершения дополнительно запрашивают `PROCESS_TERMINATE` и вызывают стандартный `TerminateProcess`.
Перед завершением имя `.exe` проверяется через тот же открытый дескриптор процесса.
Недоступные процессы завершить нельзя; повышение привилегий не запрашивается.

После обновления с GameTime выйдите из старой версии через трей, запустите OneMoreTimer и нажмите «ОК».
Если автозапуск был включён, прежняя запись заменится новым именем и путём к исполняемому файлу.

Автозапуск — значение `OneMoreTimer` в `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`.
Он запускает текущий `.exe` с аргументом `--tray`. После перемещения `.exe` выключите и снова включите автозапуск.
Чтобы удалить программу, выключите автозапуск, выйдите через tray и удалите `.exe`.
Папку с данными можно удалить отдельно, если статистика и настройки больше не нужны.

Предварительная сборка не подписана сертификатом издателя.
Нельзя гарантировать отсутствие ложных срабатываний всех антивирусов или совместимость со всеми античитами.
Не отключайте защиту ради запуска приложения. Проверку конкретных игр и экранных режимов проводите отдельно.

## Сборка из исходников

Требуется .NET 10 SDK и Windows. Основное приложение и проверочный проект не имеют сторонних PackageReference.
Для первой self-contained публикации SDK скачивает официальные пакеты среды .NET с NuGet.
Это операция сборки: готовая программа в сеть не обращается.

Из корня проекта:

```powershell
dotnet build
dotnet run --project .\src\GameTime\GameTime.csproj
dotnet build -c Release
```

Один автономный `.exe` для Windows x64:

```powershell
dotnet publish .\src\GameTime\GameTime.csproj -c Release -r win-x64 --self-contained true `
  -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -o .\artifacts\win-x64-0.6.1
```

Результат: `artifacts\win-x64-0.6.1\OneMoreTimer.exe`. Символы встроены, trimming выключен.
Среда .NET включена в `.exe`; её нативные файлы могут извлекаться в `%TEMP%\.net` при первом запуске.

Проверки без стороннего тестового фреймворка:

```powershell
dotnet run --project .\tests\GameTime.Tests\GameTime.Tests.csproj -c Release
dotnet run --project .\tests\GameTime.Tests\GameTime.Tests.csproj -c Release -- --live
dotnet run --project .\tests\GameTime.Tests\GameTime.Tests.csproj -c Release -- `
  --exe .\artifacts\win-x64-0.6.1\OneMoreTimer.exe
```

`--live` дополнительно выполняет настоящий минутный цикл мониторинга (около 63 секунд).
`--exe` проверяет, что указанный `.exe` завершает повторный запуск до доступа к данным.
Для этой проверки основное приложение должно быть закрыто.
Тесты используют собственные файлы в `artifacts\tests`, не меняют автозапуск и пользовательскую статистику.
Проверки завершения создают отдельные вспомогательные процессы и завершают только их.
Тесты интерфейса кратковременно открывают тестовые окна и сохраняют изображения трёх вкладок и состояний overlay.
Недоступная в изолированном рабочем столе проверка foreground явно отмечается как `SKIP`.

## Структура

Один WinForms-проект и небольшой исполняемый проект проверок.
`GameTracker` начисляет время по снимкам; `TimeStore` хранит дневные итоги;
`AtomicJson` отвечает за запись/восстановление; `GameDetector` и `NativeMethods` определяют игры;
`OverlayForm`, `SettingsForm` и `EditTimeForm` рисуют интерфейс; `TrayManager` управляет tray;
`GameTimeContext` связывает жизненный цикл, таймеры и события Windows;
`ProcessEnforcer` выполняет включённые правила лимитов и расписания; `BlockingSettings` хранит их параметры;
`ApplicationRulePanel`, `DurationInput`, `RuleForms` и `ScheduleEditorForm` предоставляют редакторы правил;
`StartupRegistration` включает автозапуск только по выбору пользователя.

Результаты фактически выполненных проверок записаны в `VERIFICATION.md`.

Внизу общего окна указаны автор `(c) alensp55@gmail.com` и ссылка `https://github.com/alensp55/OneMoreTimer`.
Ссылка открывается в браузере только по нажатию; само приложение не выполняет сетевых запросов.

Ссылка поддержки внизу окна открывает [CloudTips](https://pay.cloudtips.ru/p/ae5b2218) на русском
или [Buy me a coffee](https://buymeacoffee.com/alensp55) на английском, только после нажатия.
